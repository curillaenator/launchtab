const TOC_EXTENSION_NAME = 'tableOfContent';

const SCROLL_DEFAULT_OFFSET = -96 - 8;
const SCROLL_DEFAULT_DURATION = 300;

const DEFAULT_RANGE: Record<'minLevel' | 'maxLevel', number> = {
  minLevel: 1,
  maxLevel: 6,
};

export {
  DEFAULT_RANGE,
  TOC_EXTENSION_NAME,
  //
  SCROLL_DEFAULT_OFFSET,
  SCROLL_DEFAULT_DURATION,
};
